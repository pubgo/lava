# Legacy grpc_server YAML key (alias of gateway_server settings).
# Prefer gateway_server.yaml for new projects.
grpc_server:
  base_url: /welogin
  enable_print_router: true

  # 256k
#  ws_read_limit: 262144
#  grpc_port: 8000
#  http_port: 8001
